export class University {
    constructor(public id: number, public name: string,
        public completeName: string, public image: string,
        public coordinator: string, public email: string, public telephone: string) {
    }
}

